#include <stdio.h>
#include <stdlib.h>

int main(void) {
    fprintf(
        stderr,
        "scanning_rate: todavia no implementado.\n"
        "Este ejecutable queda reservado para el sistema 2 del TP4.\n"
    );
    return EXIT_FAILURE;
}
